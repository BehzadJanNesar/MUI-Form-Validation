import { Box, Stack } from "@mui/material";

export default function Layout() {
   return (
      <Stack sx={{ width: "100%", maxHeight: "100vh" }}>
         <Box>Layout</Box>
         <Box>Layout2</Box>
      </Stack>
   );
}
